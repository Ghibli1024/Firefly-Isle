# V4/
> L2 | 父级: /docs/design/Image-2/CLAUDE.md

成员清单
DESIGN.md: V4 候选评估真源，定义注意力层级、三套方向、role token、双材料、排版、surface、组件、动效、响应式、可访问性与生产迁移边界，[PROTOCOL]: 候选或合同变化时更新此文件与 brief.md
brief.md: V4 批次说明，记录三候选、同内容比较方法、2026-07-31 浏览器验证矩阵、截图交付物与“选择前不改生产页”边界，[PROTOCOL]: 候选、交付物或选择状态变化时更新
01-clinical-calm-light.png: Clinical Calm Light 的 1440 × 1000 桌面浏览器证据，[PROTOCOL]: 页面结构或候选 token 变化时重拍
02-firefly-glass-dark.png: Firefly Glass Dark 的 1440 × 1000 桌面浏览器证据，[PROTOCOL]: 页面结构或候选 token 变化时重拍
03-living-archive-light.png: Living Archive Light 的 1440 × 1000 桌面浏览器证据，[PROTOCOL]: 页面结构或候选 token 变化时重拍
04-design-preview-mobile.png: Clinical Calm Light 的 390 × 844 移动浏览器证据，[PROTOCOL]: 响应式结构变化时重拍

法则: V4 是候选评估层，不是生产主题；三个方向必须共享内容与 DOM，产品负责人明确选择前不得把候选 token 扩散到正式路由。
