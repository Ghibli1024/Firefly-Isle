# 81f0dda docs: align baseline status

## 1. 基本信息
- Commit: `81f0dda`
- Message: `docs: align baseline status`
- 时间顺序: 0006
- 日期: 2026-04-11
- 在当前仓库中的定位: 基线完成后的文档校正提交

## 2. 这次提交实际解决了什么
- 对 `CLAUDE.md`、`docs/products/spec.md`、`openspec/changes/mvp-core/{design,proposal}.md` 做了小范围文字校正。
- 它的目的不是新增功能，而是让“基线已到哪一步”的文案表述更接近真实状态。

## 3. 是怎么一步一步完成的
1. 在大基线提交之后，重新检查文档对“当前仓库状态”的描述是否和实际文件树一致。
2. 只改动最容易漂移的状态表述，不扩散到代码或更多模块文档。
3. 用 docs-only 提交把这种状态偏差单独收掉，避免它和后续实现提交缠在一起。
4. 已证实命令：无。
5. 未证实命令：没有保留任何可以直接引用的 shell 记录，只能确认最终是一笔小型文档校正提交。

## 4. 遇到的问题
- 基线推进很快时，文档状态描述最容易先漂移。
- 若不单独修，后续协作者会对“当前仓库到底做到哪”产生误判。

## 5. 证据
- `git show --stat 81f0dda`: 4 个文档文件，小范围增删改
- 只涉及文字层对齐，不涉及运行时代码

## 6. 我的判断
- 这是一个必要但非常轻量的文档收口提交。
- 它的价值在于防止后续基线误读，而不是提供新能力。

## 7. 置信度
- 当前等级: 中
- 原因: diff 清楚，但缺少更细的一手会话过程，只能按文档修正逻辑重建。
