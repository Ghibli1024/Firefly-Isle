# aea389d Revert "Ignore internal docs"

## 1. 基本信息
- Commit: `aea389d`
- Message: `Revert "Ignore internal docs"`
- 时间顺序: 0003
- 日期: 2026-03-27
- 在当前仓库中的定位: 前史 / 文档策略回滚提交

## 2. 这次提交实际解决了什么
- 这次提交把上一条 `a766dcc` 的 ignore 规则撤销，恢复了相关文档进入仓库历史的状态。
- 实际效果是：承认前一个决策不成立，让文档重新回到版本管理范围。

## 3. 是怎么一步一步完成的
1. 先确认前一条“忽略 internal docs”的判断会误伤需要保留的资料。
2. 直接采用回滚提交，而不是继续手工修改别的文件，把修正范围压缩到最小。
3. 这类提交的 message 明确带 `Revert`，高度说明当时采取的是标准回滚路径。
4. 已证实命令：无。
5. 高度推定但未证实命令：`git revert a766dcc`。当前 message 形式与 diff 结构都强烈支持这一点，但没有保留原始终端转录，因此仍不把它写成绝对事实。

## 4. 遇到的问题
- 这说明早期仓库对文档分层还没有稳定规则。
- 也提醒后续需要更明确地区分：可归档的私有杂项 vs 必须保留的项目文档真相源。

## 5. 证据
- `git show --stat aea389d`: 仅删除 `.gitignore` 中上一条引入的规则
- 与 `a766dcc` 形成前后相邻的成对历史

## 6. 我的判断
- 这是一次健康的快速纠偏，胜在及时，没有让错误的文档策略继续扩散。
- 它也为后续仓库更加重视文档真相源埋下了前提。

## 7. 置信度
- 当前等级: 中
- 原因: 回滚关系清晰、diff 明确，但缺少当时更完整的讨论上下文。
