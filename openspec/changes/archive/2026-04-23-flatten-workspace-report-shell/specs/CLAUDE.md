# specs/
> L2 | 父级: /openspec/changes/flatten-workspace-report-shell/CLAUDE.md

成员清单
app-shell/spec.md: 约束 `/app` 报告区直接显示治疗时间线表格，不再在 `TimelineTable` 外额外渲染“临床结构化报告”总标题壳，[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

法则: delta spec 只写用户可观察行为，不写 route 或 component 的具体实现。
