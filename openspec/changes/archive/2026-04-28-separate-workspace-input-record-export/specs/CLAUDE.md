# specs/
> L2 | 父级: ../CLAUDE.md

成员清单
app-shell/spec.md: app-shell delta spec，定义 `/app` 作为输入提取台、文件/语音输入工具与导出迁移边界，[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
export/spec.md: export delta spec，定义 PDF/PNG 用户入口归属 `/record/:id`，并保留现有导出链路语义，[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

法则: specs 只描述行为合同，不描述视觉稿实现细节。
