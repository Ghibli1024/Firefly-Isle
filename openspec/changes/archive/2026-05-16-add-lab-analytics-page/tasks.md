## 1. 数据模型与迁移

- [x] 1.1 新增 Supabase 迁移：创建 `lab_report_batches`，包含患者归属、分类、检查日期、源文件元数据、OCR 文本、复核状态、时间戳、索引和 RLS policy。
- [x] 1.2 扩展 `lab_results`，增加可空 / 默认的批次关联和派生指标元数据，同时保持已有读写兼容。
- [x] 1.3 更新 TypeScript 领域类型和持久化 mapper，支持实验室批次、派生读数和批次关联实验室读数。
- [x] 1.4 增加 schema / persistence 测试，覆盖批次所有权、实验室读数批次映射、派生元数据和旧数据兼容读取。

## 2. 实验室字典与趋势逻辑

- [x] 2.1 基于 `update-followup-data` 映射新增血常规、血生化、肿瘤标志物显式字典。
- [x] 2.2 实现 OCR candidate 到字典项的归一化，输出稳定的 item code、名称、单位和参考范围。
- [x] 2.3 实现血常规 NLR、PLR、MLR 派生指标计算，带派生元数据和缺失值保护。
- [x] 2.4 扩展实验室趋势工具，支持分类最近异常汇总、图表序列输出、肿瘤标志物两段连续上涨检测。
- [x] 2.5 增加纯逻辑单元测试，覆盖字典映射、参考范围解析、CBC 派生指标、最近异常汇总、图表序列和肿瘤标志物上涨检测。

## 3. 网页端实验室报告摄入

- [x] 3.1 新增网页端实验室报告上传入口，支持血常规、血生化、肿瘤标志物，不依赖本地 Excel。
- [x] 3.2 复用 OCR 边界识别上传的 lab 图片 / PDF，失败时可恢复且不污染当前患者记录。
- [x] 3.3 增加结构化确认表格，让用户保存前可以编辑日期、指标映射、数值、单位和参考范围。
- [x] 3.4 确认后持久化为一条 `lab_report_batches` 加多条关联 `lab_results`。
- [x] 3.5 检测同一患者、同一分类、同一日期的已有批次，并在写入重复数据前要求用户选择替换或取消。
- [x] 3.6 增加聚焦测试，覆盖摄入空态、OCR candidate 复核、保存前修正、未解析行、重复检测和保存 payload。

## 4. 统计页面 UI

- [x] 4.1 新增统计路由，把侧栏 `统计` 从敬请期待动作替换为真实导航和激活态。
- [x] 4.2 按选定深色临床控制塔基准图和现有 V3 system token 搭建页面骨架。
- [x] 4.3 渲染顶部摘要面板：最近异常、肿瘤标志物连续上涨提醒、覆盖指标。
- [x] 4.4 渲染分类 tabs 和指标列表，包含最新值、状态、日期和可访问文字标签。
- [x] 4.5 渲染选中指标趋势图、参考范围提示、最新值标记和等价数据表。
- [x] 4.6 渲染底部面板：最近异常读数、肿瘤标志物连续上涨提醒。
- [x] 4.7 增加统计页空态、加载态、导入 / 复核态、错误态和无风险状态。
- [x] 4.8 增加响应式处理：移动端使用堆叠摘要、分类 tabs、指标选择、全宽图表和可折叠监测面板。

## 5. 安全、可访问性与验证

- [x] 5.1 确保所有趋势提醒使用非诊断文案，不输出进展结论、诊断、用药或治疗建议。
- [x] 5.2 确保图表状态不只靠颜色表达，提供偏高 / 偏低 / 正常 / 上涨等文字标签和等价表格。
- [x] 5.3 增加路由 / 组件测试，覆盖侧栏导航、统计页渲染、分类切换、指标选择、图表表格等价和监测面板。
- [x] 5.4 实现期间同步更新 GEB 地图，覆盖新增 routes、components、libs、migrations 和 OpenSpec artifacts。
- [x] 5.5 完成前运行聚焦测试、`git diff --check`、`openspec validate --all` 和相关应用验证命令。

## 6. 入口调整与演示统计页

- [x] 6.1 把统计页上传表单移出 `/analytics`，让文件上传继续由 `/app` 输入区承接。
- [x] 6.2 新增 `/analytics/demo` 演示统计页，使用独立实验室趋势 fixture，不污染 `/record/demo`。
- [x] 6.3 将真实统计页路由改为 `/analytics/:id`，并让侧栏在有真实记录时指向记录统计、无输入时指向 demo 统计。
- [x] 6.4 更新组件 / 路由 / 合同测试、OpenSpec 文档和 GEB 地图，保持选定深色控制塔布局与 V3 设计系统边界。
