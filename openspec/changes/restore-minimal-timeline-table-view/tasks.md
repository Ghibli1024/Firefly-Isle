## 1. OpenSpec 合同

- [x] 1.1 完成 proposal、design、tasks、timeline-table/export delta spec。
- [x] 1.2 运行 `openspec validate --all`。

## 2. 视图接入

- [x] 2.1 扩展 RecordViewMode 为 dossier/table/gantt。
- [x] 2.2 在 record view switch 中增加极简表格选项。
- [x] 2.3 在 `/record/:id` 和 `/record/demo` 渲染 `TimelineTable` 视图。
- [x] 2.4 确保编辑保存、关键字段高亮和三类患者渲染不回退。

## 3. 验证与文档

- [x] 3.1 更新 record page / timeline table 测试。
- [x] 3.2 更新 GEB `CLAUDE.md` 与 `docs/products` 状态。
- [x] 3.3 运行相关测试、`npm run type-check`、`openspec validate --all`。
